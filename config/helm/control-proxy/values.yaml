# Default values for control-proxy.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# This will set the replicaset count more information can be found here: https://kubernetes.io/docs/concepts/workloads/controllers/replicaset/
replicaCount: 1

fullNameOverride: "control-proxy"

# This sets the container image more information can be found here: https://kubernetes.io/docs/concepts/containers/images/
image:
  repository: ghcr.io/githedgehog/fabricator/control-proxy
  # This sets the pull policy for images.
  pullPolicy: IfNotPresent

#This section builds out the service account more information can be found here: https://kubernetes.io/docs/concepts/security/service-accounts/
# This is for setting Kubernetes Annotations to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/
podAnnotations: {}
# This is for setting Kubernetes Labels to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
podLabels: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

config: |
  User 65532
  Group 65532
  Port 3128
  LogLevel Connect
  MaxClients 5000
  Filter "/conf/filters.conf"
  FilterDefaultDeny Yes
  FilterType fnmatch
  StatFile "/conf/stats.conf"

statFile: |
  # HELP tinyproxy_open_connections Currently open client connections.
  # TYPE tinyproxy_open_connections gauge
  tinyproxy_open_connections {opens}
  # HELP tinyproxy_requests_total Total requests processed.
  # TYPE tinyproxy_requests_total counter
  tinyproxy_requests_total {reqs}
  # HELP tinyproxy_bad_connections_total Bad/aborted connections.
  # TYPE tinyproxy_bad_connections_total counter
  tinyproxy_bad_connections_total {badconns}
  # HELP tinyproxy_denied_total Connections denied by ACL/filter/auth.
  # TYPE tinyproxy_denied_total counter
  tinyproxy_denied_total {deniedconns}
  # HELP tinyproxy_refused_total Connections refused due to load ceiling.
  # TYPE tinyproxy_refused_total counter
  tinyproxy_refused_total {refusedconns}
  # HELP tinyproxy_build_info Build information for the running tinyproxy.
  # TYPE tinyproxy_build_info gauge
  tinyproxy_build_info{version="{version}"} 1

urls: []

# This is for setting up a service more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/
service:
  type: NodePort
  port: 3128
  nodePort: 31028

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

nodeSelector: {}

tolerations: []

affinity: {}

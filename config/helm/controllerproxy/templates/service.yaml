apiVersion: v1
kind: Service
metadata:
  name: {{ include "controllerproxy.fullname" . }}
  labels:
    {{- include "controllerproxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http-proxy
      nodePort: {{ .Values.service.nodePort }}
      protocol: TCP
      name: tiny-proxy
  selector:
    {{- include "controllerproxy.selectorLabels" . | nindent 4 }}
